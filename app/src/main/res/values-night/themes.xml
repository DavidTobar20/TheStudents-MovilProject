<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Variante nocturna del tema de ventana. Ver comentario en values/themes.xml. -->
    <style name="Theme.TheStudents" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/window_primary_dark</item>
        <item name="colorOnPrimary">@color/window_on_primary_dark</item>
        <item name="android:windowBackground">@color/window_background_dark</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>
